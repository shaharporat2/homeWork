 export class Perfume{

    private _name: string;
    private _company: string;
    private _ml: number;
    private _gender: string;
    private _price: number;

    constructor(name: string, company: string, ml: number, gender: string, price: number){

        this._name = name;
        this._company = company;
        this._ml = ml
        this._gender = gender;
        this._price = price;

    }
}
